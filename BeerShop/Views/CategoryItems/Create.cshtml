@model BeerShop.Models.CategoryItemHelper


@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

    @using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>CategoryItem</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.categoryitem.name)
        </div>

        <div class="editor-field">
            @Html.EditorFor(model => model.name)
            @Html.ValidationMessageFor(model => model.categoryitem.name)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.categoryitem)
        </div>

        <div class="editor-field">
            @Html.DropDownListFor(model => model.SelectedCategory, ViewBag.categoriesList as SelectList)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}


